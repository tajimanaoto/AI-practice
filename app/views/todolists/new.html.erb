<h1>新規投稿</h1>
<%= form_with model: @list, url:'/todolists', local:true do |f| %>
  <h4>題名</h4>
  <%= f.text_field :title %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <h4>picture</h4>
  <%= f.attachment_field :image %>
  <%= f.submit '投稿' %>
<% end %>